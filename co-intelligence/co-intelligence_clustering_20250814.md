**Prompt for LLM Model: RNA Data Clustering and Splitting Script Generation**

**Overall Goal:** Generate a Python script to perform RNA data clustering based on both sequence and tertiary structure similarity, followed by a cluster-based splitting of the dataset into training, validation, and testing sets. The script should be robust, modular, and adhere to the methodologies described in the provided research sources.

**Input Data:**
*   **PDB files:** Located in the PDB_DIR = `data/experiments_data/exp2.2_manuscript/dataset_20250731/extracted_pdbs` directory. Each file represents a 3D RNA tertiary structure (e.g., `1ABC.pdb`).
*   **FASTA files:** Located in the FASTA_DIR = `data/experiments_data/exp2.2_manuscript/dataset_20250731/extracted_sequences` directory. Each file contains the nucleotide sequence for a corresponding PDB structure (e.g., `1ABC.fasta`).
*   **Assumption:** Assume a one-to-one correspondence between PDB files and FASTA files based on their base filename (PDB ID). The PDB ID will be used as the unique identifier for each RNA sample.

**Install External Tools (Guide to install them and make accessible in the system's PATH):**
*   **PSI-CD-HIT:** A command-line tool for clustering sequences at high identity.
*   **US-align:** A command-line tool for universal structure alignment, used here to calculate TM-scores between RNA 3D structures.

**Required External Tools (assume these are pre-installed and accessible in the system's PATH):**
*   **PSI-CD-HIT:** A command-line tool for clustering sequences at high identity.
*   **US-align:** A command-line tool for universal structure alignment, used here to calculate TM-scores between RNA 3D structures.

**Output:**
For each clustering strategy (sequence and structure) and each specified threshold, the script should output separate files (e.g., `.txt` or `.csv`) containing the PDB IDs assigned to the `train`, `validation`, and `test` sets. The file names should clearly indicate the clustering method and threshold (e.g., `seq_0.8_train.txt`, `struct_0.45_test.txt`).

**Detailed Steps for Python Script Implementation:**

**1. Data Collection and Initial Preprocessing:**
    *   **Identify All RNA Samples:**
        *   Scan the $PDB_DIR directory to collect all PDB filenames. Extract the base filename (PDB ID) for each as the unique identifier.
        *   Verify that a corresponding FASTA file exists in $FASTA_DIR for each identified PDB ID. If not, log a warning and skip that sample.
    *   **RNA Length Filtering:**
        *   Based on RDesign, filter out any RNA structures/sequences that are **longer than $MAX_SEQ_LEN (make it a parameter) nucleotides**. This is crucial for managing computational resources and aligning with the dataset preparation described in the sources. The script should read the sequence length from the FASTA files for this filtering step.
        *   Create a consolidated list of PDB IDs that pass this length filter, along with their corresponding PDB and FASTA file paths.

**2. Sequence Similarity Clustering (using PSI-CD-HIT):**
    *   **Prepare Input:** Create a single multi-FASTA file (e.g., `all_rna_sequences.fasta`) containing all filtered RNA sequences.
    *   **Execute PSI-CD-HIT:** Use Python's `subprocess` module to run `psicd-hit` on the prepared multi-FASTA file.
        *   **Apply Multiple Thresholds:** Run `psicd-hit` for each of the following sequence similarity thresholds: **0.8, 0.9, and 1.0**.
        *   **Command Construction:** Ensure the `psicd-hit` command correctly specifies the input file, output prefix for cluster files, and the respective similarity threshold (e.g., `-c 0.8`).
    *   **Parse PSI-CD-HIT Output:**
        *   For each `.clstr` file generated by `psicd-hit`:
            *   Parse the file to identify clusters. Each cluster is defined by a representative sequence and its members.
            *   Store the clusters as lists of PDB IDs (e.g., a dictionary where keys are cluster IDs/representatives and values are lists of member PDB IDs).

**3. Structure Similarity Clustering (using US-align and Graph-based Clustering):**
    *   **Calculate TM-score Matrix:**
        *   For all unique PDB structures that passed the initial length filter, compute the **TM-score** for every pair of structures.
        *   **Execute US-align:** Use Python's `subprocess` module to run `US-align` for each pair of PDB files. The output should provide the TM-score.
        *   **Store Results:** Store these pairwise TM-scores in a dictionary or a symmetric matrix, mapping (PDB_ID1, PDB_ID2) to their TM-score.
    *   **Perform Graph-based Clustering:**
        *   **Construct Graph:** For each specified structural similarity threshold, create an undirected graph where each RNA structure (PDB ID) is a node. An **edge** is drawn between two nodes if their calculated **TM-score exceeds the given threshold**.
        *   **Identify Clusters:** Find the **connected components** within this graph. Each connected component represents a structural cluster.
        *   **Apply Multiple Thresholds:** Perform this graph-based clustering for the following TM-score thresholds: **0.6, 0.5, and 0.4** (from RiboDiffusion), and additionally **0.45** (from RDesign). The script should be flexible enough to iterate through these.
        *   **Libraries:** Utilize a library like `networkx` for graph construction and finding connected components.

**4. Data Splitting Strategy (Cluster-based Allocation):**
    *   **Apply to Each Set of Clusters:** The splitting logic should be applied independently to the clusters obtained from sequence similarity (for each threshold) and structural similarity (for each threshold).
    *   **Core Principle: Allocate Entire Clusters:** **Ensure that entire clusters are allocated** to either the training, validation, or testing sets. No single RNA sample (PDB ID) or cluster should appear in more than one split set.
    *   **Target Ratios (Approximate):** Aim for a split distribution of approximately **15% for the testing set, 10% for the validation set, and the remaining 75% for the training set**. Since entire clusters are allocated, the exact percentages might vary slightly.
    *   **Length Distribution Balancing:**
        *   Calculate the **average length of all RNA sequences** in the entire dataset (after length filtering).
        *   Implement a **sequential allocation strategy** for clusters to the train, validation, and test sets. This means iterating through the clusters and assigning them to the sets in a way that attempts to keep the *average sequence length within each split set* as close as possible to the overall average length. This might involve tracking the current average length of each accumulating split set.
    *   **Handling Large Clusters:** Any cluster containing **more than 30 RNA samples** must be assigned **exclusively to the training set** to prevent bias in evaluation. The script should prioritize placing these clusters into the training set before performing the general allocation.
    *   **Output Split Files:** For each clustering type and threshold, write the PDB IDs for the train, validation, and test sets into separate, clearly named output files.

**Python Script Best Practices:**
*   **Modularity:** Break down the script into well-defined functions (e.g., `filter_by_length`, `run_psicd_hit`, `parse_psicd_hit_clusters`, `calculate_tm_matrix`, `perform_graph_clustering`, `split_dataset`).
*   **Error Handling:** Implement `try-except` blocks for file I/O, `subprocess` calls, and potential data parsing issues.
*   **Comments:** Add extensive comments to explain the logic, especially for the clustering and splitting algorithms.
*   **Logging:** Use Python's `logging` module to provide informative messages about progress, skipped files, and errors.
*   **Dependencies:** Clearly state required Python libraries (e.g., `os`, `glob`, `subprocess`, `pandas`, `numpy`, `scipy.cluster.hierarchy`, `networkx`).
